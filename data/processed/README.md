Processed analytical tables are reproducible artifacts generated
via SQL transformations and Python notebooks. They are excluded
from version control to ensure code remains the single source of truth.